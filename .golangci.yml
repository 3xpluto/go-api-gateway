run:
  timeout: 5m

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - gosimple
    - unused
    - revive

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
